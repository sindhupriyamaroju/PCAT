Operators
Create a program that simulates a simple calculator using command-line arguments to perform and print the result of
addition, subtraction, multiplication, and division..

package com;
import java.util.Scanner;
public class calculator
{
	public static void main(String[] args)
{
		int n1=Integer.valueOf(args[0]);
		int n2=Integer.valueOf(args[1]);
		
		int sum=findsum(n1,n2);
		  System.out.println("sum");
		int sub=findsub(n1,n2);
		  System.out.println("sub");
		int mult=findmul(n1,n2);
		  System.out.println(mul);
		int div=finddiv(n1,n2);
		  System.out.println(div);
		}
	public static int findsum(int n1,int n2)
           {
		int sum=n1+n2;
		return sum;
	   }
	public static int findsub(int n1,int n2)
           {
		int sub=n1-n2;
		return sub;		
            }
	public static int findmul(int n1,int n2
            {
		int mul=n1*n2;
		return mul;	
            }
	public static float finddiv(int n1,int n2)
            {
		float div=num1/num2;
		return div;
            }
}

